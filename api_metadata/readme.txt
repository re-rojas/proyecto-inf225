Para el funcionamiento de esta api se utiliza:

- Python 3.11
    * librerias a instalar en requirements.txt ("pipenv install -r requirements.txt")

- MongoDB 6.0


Correr api (por lo menos en Windows) con "python -m uvicorn main:app --reload"